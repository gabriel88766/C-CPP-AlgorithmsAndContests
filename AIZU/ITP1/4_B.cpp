#include<bits/stdc++.h>

const double PI = acos(-1.);
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    double r;
    cin >> r;
    cout << setprecision(10) << fixed << PI*r*r << " " << 2*PI*r << "\n";
}